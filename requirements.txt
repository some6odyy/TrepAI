# Versiones mínimas en vez de fijas (==): las que estaban antes eran de
# antes de que existieran wheels precompilados para Python 3.14, así que
# pip intentaba compilarlas desde el código fuente (necesita Rust/C++
# instalado) y fallaba en máquinas sin ese toolchain — típico en salas de
# la universidad. Con >= dejamos que pip resuelva la versión más nueva
# compatible, que ya viene con wheel listo para instalar en 3.11–3.14.
fastapi>=0.117.1
uvicorn[standard]>=0.30.0
gunicorn>=23.0.0
sqlalchemy>=2.0.52
psycopg[binary]>=3.2.3
pydantic>=2.13.4
email-validator>=2.2.0
python-dotenv>=1.0.1
bcrypt>=5.0.0
python-jose[cryptography]>=3.3.0
requests>=2.32.3
